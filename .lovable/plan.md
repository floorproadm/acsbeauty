
User wants to speed up service selection in the modal. Looking at current flow: Service dropdown → Technique dropdown (if exists) → Option/SKU dropdown (if exists). That's up to 3 separate dropdowns to navigate.

Best UX improvement: replace cascading dropdowns with a single searchable command palette showing all SKUs grouped by service, with duration and price visible. Type to filter instantly.

Let me implement: replace the 3 selects (service/variation/sku) with one searchable Combobox that shows the full hierarchy as a flat searchable list.
